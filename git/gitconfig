[user]
        name = Iñigo Medina
        email = imedina@grosshat.com
        signingkey = 58B65847
[color]
        ui = auto
[color "branch"]
        current = yellow reverse
        local = yellow 
        remote = magenta
[color "diff"]
        meta = blue bold
        frag = magenta bold
        old = red reverse
        new = green reverse
        whitespace = white reverse
[color "status"]
        added = yellow
        changed = green
        untracked = cyan reverse
        branch = magenta
[core]
        whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
        editor = emacs
[alias]
        st = status
        ci = commit -S
        br = branch
        co = checkout
        df = diff
        lg = log -p
        lol = log --graph --decorate --pretty=oneline --abbrev-commit
        lola = log --graph --decorate --pretty=oneline --abbrev-commit --relative-date --all
        ls = ls-files
[push]
        default = simple
[commit]
	gpgsign = true
